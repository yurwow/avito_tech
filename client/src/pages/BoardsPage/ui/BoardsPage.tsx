import { ProjectsList } from '@/widgets/ProjectList';
import { Box } from '@mui/material';

const BoardsPage = () => {
    return (
        <Box>
            <ProjectsList />
        </Box>
    );
};

export default BoardsPage;
